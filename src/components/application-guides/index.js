import { createAnNft } from './create-an-nft'
import { gallery } from './gallery'
import { staking } from './staking'


export const guides = [
  createAnNft,
  staking,
  gallery
  
]
//.sort((a, b) => a.name.localeCompare(b.name))
